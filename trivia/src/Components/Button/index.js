// state 
    // object: property of difficultyState/genreState & toggle 
        // {medium : true} / {General: true}
    // buttonUseability:  button allowed to work - true/false


//function 
    //handleToggle - set the states to true/false 
    //handleUseability - turn off the other buttons 

// props to receive  
    // Text (answer/Difficulty/Genre)

// Return 
    //handleToggle function (handles on / off onClick)

{/* <button onClick={handleToggle}><</button> */}
