function DisplayAnswers({state, dispatch, correctAnswer, incorrectAnswers}) {
    // store answers in an array
    // shuffle array order
    return(
        <div>
            <button></button>
        </div>
    )
}

export default DisplayAnswers;